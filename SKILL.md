---
name: bili-download
description: 把你支持/关注的 B站创作者内容完整归档到本地 —— 按合集分类、序号命名、增量更新。当用户要"下载/备份/归档某UP主的B站合集、系列、视频"、给出 space.bilibili.com 或视频链接、想离线留存充电支持的系列内容(不受在线时长限制)时触发。支持单视频与整合集,扫码登录后可完整归档已充电/包月的系列。
---

# bili-download — B站创作者内容归档技能

把你**支持、关注的 B站创作者内容**完整、有序地留在本地:单视频或整合集,按合集分目录、序号命名、增量更新。已经充电支持的系列课程/合集,扫码登录后也能离线慢慢看,不受在线时长限制。

## 何时触发

- "下载/备份/归档 XX 的 B站合集/系列/视频" / "归档我关注的博主"
- 给出 `space.bilibili.com/<UID>/lists` 或视频链接
- 各类归档场景:
  - 防删除/防和谐备份(UP主删稿、视频下架前抢救)
  - UP主停更/注销前存档
  - 离线观看(通勤、断网、省流量)
  - 系列课程 / 知识合集本地化,反复学习
  - 充电支持的系列离线留存,慢慢看
  - 二创 / 剪辑素材留存
  - 收藏夹 / 追更系列归档

## 画质说明

- 默认取匿名/普通登录可得的最高画质
- **cookie 若为大会员账号**,可下 1080P高码率 / 4K 等高画质(前提:原视频本身支持该清晰度)

## 前置(仅首次)

1. **扫码登录**(合集下载必需,arc/search 接口对匿名返风控):跑 `node login.js` → 手机 B站 App 扫码 → 自动生成 `cookies.txt`
2. **依赖二进制**:`yt-dlp.exe` / `ffmpeg.exe` / `ffprobe.exe` 放脚本同目录(可用 install 脚本自动下)

## 执行流程

1. 从用户输入提取 UP 主 UID(数字,或从 space URL 解析)
2. 跑:`node bili_download.js <UID> [--out <目录>] [--cookie <文件>]`
   - `--out`:下载目录,默认 `bili_downloads`。**用户指定目录时用此参数传入**
   - `--cookie`:cookie 文件,默认 `cookies.txt`
   - skill(非交互)场景脚本自动走默认值,不会阻塞等输入
3. 脚本自动:
   - **cookie 预检**:启动先验 isLogin,过期立即提示重扫(不会白跑)
   - 若无 cookie:提示先跑 `login.js`(合集下载必需)
   - wbi 签名 + cookie 调 `arc/search`(全部视频) + `view`(合集归属) → 聚合 season_id
   - `seasons_archives_list` 拿每个**合集内视频(合集定义顺序)**
   - 按合集建目录,`序号_标题.mp4` 命名
   - **增量**:已下的(记录在 `downloaded.json`)自动跳过,只下新的
4. 报告:X 合集 / Y 视频 / 新下N / 跳过M / 失败K

## 产物结构

```
bili_downloads/
├── 合集名/
│   ├── 01_标题.mp4
│   ├── 02_标题.mp4
│   └── ...
├── 结构.json        # 合集元数据
└── downloaded.json  # 增量记录(已下bvid)
```

## 常见失败处理

| 现象 | 原因 | 处理 |
|---|---|---|
| `-352 风控` | 无 cookie 或过期 | 跑 `node login.js` 重新扫码 |
| 只下到 3 分钟预览 | 付费视频 + cookie 无购买权限 | 确认账号已购;重跑 login.js |
| `10054 连接重置` | 临时网络 | 重跑(增量自动补失败的) |
| 中文目录/文件名"乱码" | 仅控制台**显示**乱码,文件系统**实际正确** | 忽略,看实际文件 |

## 定时增量(可选)

配合 cron/计划任务定期跑 `node bili_download.js <UID>`,自动补 UP 主新发的视频(增量跳过已下)。
